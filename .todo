scheda:
    - info (serialId, nome, versione fw, tipo hw, ultimo ping, proprietario, ecc...)
    - stato (online/offline/errore, CPU, TEMP, MEM, ecc...)
    - dispositivi (I/O)
    - pins (read-only)

pin:
    - info (nome, numero, tipo, mode)
    - valore
    - metadata (tensione, corrente massima, ecc...)

module:
    - info (serialId, nome, tipo, sottotipo, driver, pins, ecc...)
    - stato (valore, unit, ecc...)
    - config (ciclo lettura, calibrazioni, ecc...)

generazione sketch:
    - generazione codice

cloud functions:
    - elaborazione lato cloud per funzioni complesse