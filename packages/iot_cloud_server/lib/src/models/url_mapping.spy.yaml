class: UrlMapping
table: url_mapping
fields:
  shortCode: String
  originalUrl: String
  createdAt: DateTime?, default=now
indexes:
  url_mapping_short_code_idx:
    fields: shortCode
    unique: true