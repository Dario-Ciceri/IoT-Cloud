class: PlatformioProject
table: platformio_project
fields:
    name: String
    path: String
    description: String?
    board: String?
    framework: String?
    createdAt: DateTime, default=now
    updatedAt: DateTime, default=now